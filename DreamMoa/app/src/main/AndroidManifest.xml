<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="twojeong.dreammoa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAcz2mJmNL99mMLrt7HhuDBZEOHokDMnYY" />

        <activity android:name=".main.MainActivity"></activity>
        <activity android:name=".search.StoreSearchActivity" />
        <activity android:name=".location.MyLocationSearchActivity" />
        <activity android:name=".search.detailSearchActivity" />
        <activity android:name=".record.AddUsageRecordActivity" />
        <activity android:name=".util.BaseActivity" />
        <activity android:name=".record.UsageRecordActivity" />
        <activity android:name=".record.UserInfoActivity" />
        <activity android:name=".faq.FaqActivity" />
        <!-- splash -->
        <activity
            android:name=".util.SplashActivity"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- splash -->

    </application>

</manifest>